// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.180.0
//   protoc               v3.21.12
// source: custom.proto

/* eslint-disable */
import * as _m0 from "protobufjs/minimal";

export const protobufPackage = "zmk.custom";

export interface ListCustomSubsystemRequest {
}

export interface ListCustomSubsystemResponse {
  subsystems: CustomSubsystemInfo[];
}

export interface CustomSubsystemInfo {
  /** Device specific subsystem index to identify the subsystem in request/response */
  index: number;
  /** Unique subsystem identifier */
  identifier: string;
  /** Subsystem UI URLs which supports interaction with the subsystem */
  uiUrl: string[];
}

export interface CallRequest {
  /**
   * target subsystem index to deliver the payload
   * The value is the same to CustomSubsystemInfo.index. The value can change per compile and maybe even at startup.
   */
  subsystemIndex: number;
  payload: Uint8Array;
}

export interface CallResponse {
  /**
   * target subsystem index to deliver the payload
   * The value is the same to CustomSubsystemInfo.index. The value can change per compile and maybe even at startup.
   */
  subsystemIndex: number;
  payload: Uint8Array;
}

export interface CustomNotification {
  /**
   * target subsystem index to deliver the payload
   * The value is the same to CustomSubsystemInfo.index. The value can change per compile and maybe even at startup.
   */
  subsystemIndex: number;
  payload: Uint8Array;
}

export interface Request {
  listCustomSubsystems?: ListCustomSubsystemRequest | undefined;
  call?: CallRequest | undefined;
}

export interface Response {
  listCustomSubsystems?: ListCustomSubsystemResponse | undefined;
  call?: CallResponse | undefined;
}

export interface Notification {
  customNotification?: CustomNotification | undefined;
}

function createBaseListCustomSubsystemRequest(): ListCustomSubsystemRequest {
  return {};
}

export const ListCustomSubsystemRequest = {
  encode(_: ListCustomSubsystemRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListCustomSubsystemRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCustomSubsystemRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListCustomSubsystemRequest {
    return {};
  },

  toJSON(_: ListCustomSubsystemRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListCustomSubsystemRequest>, I>>(base?: I): ListCustomSubsystemRequest {
    return ListCustomSubsystemRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListCustomSubsystemRequest>, I>>(_: I): ListCustomSubsystemRequest {
    const message = createBaseListCustomSubsystemRequest();
    return message;
  },
};

function createBaseListCustomSubsystemResponse(): ListCustomSubsystemResponse {
  return { subsystems: [] };
}

export const ListCustomSubsystemResponse = {
  encode(message: ListCustomSubsystemResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.subsystems) {
      CustomSubsystemInfo.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ListCustomSubsystemResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCustomSubsystemResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.subsystems.push(CustomSubsystemInfo.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCustomSubsystemResponse {
    return {
      subsystems: globalThis.Array.isArray(object?.subsystems)
        ? object.subsystems.map((e: any) => CustomSubsystemInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListCustomSubsystemResponse): unknown {
    const obj: any = {};
    if (message.subsystems?.length) {
      obj.subsystems = message.subsystems.map((e) => CustomSubsystemInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListCustomSubsystemResponse>, I>>(base?: I): ListCustomSubsystemResponse {
    return ListCustomSubsystemResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListCustomSubsystemResponse>, I>>(object: I): ListCustomSubsystemResponse {
    const message = createBaseListCustomSubsystemResponse();
    message.subsystems = object.subsystems?.map((e) => CustomSubsystemInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCustomSubsystemInfo(): CustomSubsystemInfo {
  return { index: 0, identifier: "", uiUrl: [] };
}

export const CustomSubsystemInfo = {
  encode(message: CustomSubsystemInfo, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.identifier !== "") {
      writer.uint32(18).string(message.identifier);
    }
    for (const v of message.uiUrl) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CustomSubsystemInfo {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCustomSubsystemInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.index = reader.uint32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.identifier = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.uiUrl.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CustomSubsystemInfo {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      identifier: isSet(object.identifier) ? globalThis.String(object.identifier) : "",
      uiUrl: globalThis.Array.isArray(object?.uiUrl) ? object.uiUrl.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: CustomSubsystemInfo): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.identifier !== "") {
      obj.identifier = message.identifier;
    }
    if (message.uiUrl?.length) {
      obj.uiUrl = message.uiUrl;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CustomSubsystemInfo>, I>>(base?: I): CustomSubsystemInfo {
    return CustomSubsystemInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CustomSubsystemInfo>, I>>(object: I): CustomSubsystemInfo {
    const message = createBaseCustomSubsystemInfo();
    message.index = object.index ?? 0;
    message.identifier = object.identifier ?? "";
    message.uiUrl = object.uiUrl?.map((e) => e) || [];
    return message;
  },
};

function createBaseCallRequest(): CallRequest {
  return { subsystemIndex: 0, payload: new Uint8Array(0) };
}

export const CallRequest = {
  encode(message: CallRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.subsystemIndex !== 0) {
      writer.uint32(8).uint32(message.subsystemIndex);
    }
    if (message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CallRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.subsystemIndex = reader.uint32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.payload = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallRequest {
    return {
      subsystemIndex: isSet(object.subsystemIndex) ? globalThis.Number(object.subsystemIndex) : 0,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: CallRequest): unknown {
    const obj: any = {};
    if (message.subsystemIndex !== 0) {
      obj.subsystemIndex = Math.round(message.subsystemIndex);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallRequest>, I>>(base?: I): CallRequest {
    return CallRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallRequest>, I>>(object: I): CallRequest {
    const message = createBaseCallRequest();
    message.subsystemIndex = object.subsystemIndex ?? 0;
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCallResponse(): CallResponse {
  return { subsystemIndex: 0, payload: new Uint8Array(0) };
}

export const CallResponse = {
  encode(message: CallResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.subsystemIndex !== 0) {
      writer.uint32(8).uint32(message.subsystemIndex);
    }
    if (message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CallResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCallResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.subsystemIndex = reader.uint32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.payload = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CallResponse {
    return {
      subsystemIndex: isSet(object.subsystemIndex) ? globalThis.Number(object.subsystemIndex) : 0,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: CallResponse): unknown {
    const obj: any = {};
    if (message.subsystemIndex !== 0) {
      obj.subsystemIndex = Math.round(message.subsystemIndex);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallResponse>, I>>(base?: I): CallResponse {
    return CallResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallResponse>, I>>(object: I): CallResponse {
    const message = createBaseCallResponse();
    message.subsystemIndex = object.subsystemIndex ?? 0;
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function createBaseCustomNotification(): CustomNotification {
  return { subsystemIndex: 0, payload: new Uint8Array(0) };
}

export const CustomNotification = {
  encode(message: CustomNotification, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.subsystemIndex !== 0) {
      writer.uint32(8).uint32(message.subsystemIndex);
    }
    if (message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CustomNotification {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCustomNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.subsystemIndex = reader.uint32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.payload = reader.bytes();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CustomNotification {
    return {
      subsystemIndex: isSet(object.subsystemIndex) ? globalThis.Number(object.subsystemIndex) : 0,
      payload: isSet(object.payload) ? bytesFromBase64(object.payload) : new Uint8Array(0),
    };
  },

  toJSON(message: CustomNotification): unknown {
    const obj: any = {};
    if (message.subsystemIndex !== 0) {
      obj.subsystemIndex = Math.round(message.subsystemIndex);
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CustomNotification>, I>>(base?: I): CustomNotification {
    return CustomNotification.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CustomNotification>, I>>(object: I): CustomNotification {
    const message = createBaseCustomNotification();
    message.subsystemIndex = object.subsystemIndex ?? 0;
    message.payload = object.payload ?? new Uint8Array(0);
    return message;
  },
};

function createBaseRequest(): Request {
  return { listCustomSubsystems: undefined, call: undefined };
}

export const Request = {
  encode(message: Request, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.listCustomSubsystems !== undefined) {
      ListCustomSubsystemRequest.encode(message.listCustomSubsystems, writer.uint32(10).fork()).ldelim();
    }
    if (message.call !== undefined) {
      CallRequest.encode(message.call, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Request {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.listCustomSubsystems = ListCustomSubsystemRequest.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.call = CallRequest.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Request {
    return {
      listCustomSubsystems: isSet(object.listCustomSubsystems)
        ? ListCustomSubsystemRequest.fromJSON(object.listCustomSubsystems)
        : undefined,
      call: isSet(object.call) ? CallRequest.fromJSON(object.call) : undefined,
    };
  },

  toJSON(message: Request): unknown {
    const obj: any = {};
    if (message.listCustomSubsystems !== undefined) {
      obj.listCustomSubsystems = ListCustomSubsystemRequest.toJSON(message.listCustomSubsystems);
    }
    if (message.call !== undefined) {
      obj.call = CallRequest.toJSON(message.call);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Request>, I>>(base?: I): Request {
    return Request.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Request>, I>>(object: I): Request {
    const message = createBaseRequest();
    message.listCustomSubsystems = (object.listCustomSubsystems !== undefined && object.listCustomSubsystems !== null)
      ? ListCustomSubsystemRequest.fromPartial(object.listCustomSubsystems)
      : undefined;
    message.call = (object.call !== undefined && object.call !== null)
      ? CallRequest.fromPartial(object.call)
      : undefined;
    return message;
  },
};

function createBaseResponse(): Response {
  return { listCustomSubsystems: undefined, call: undefined };
}

export const Response = {
  encode(message: Response, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.listCustomSubsystems !== undefined) {
      ListCustomSubsystemResponse.encode(message.listCustomSubsystems, writer.uint32(10).fork()).ldelim();
    }
    if (message.call !== undefined) {
      CallResponse.encode(message.call, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Response {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.listCustomSubsystems = ListCustomSubsystemResponse.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.call = CallResponse.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Response {
    return {
      listCustomSubsystems: isSet(object.listCustomSubsystems)
        ? ListCustomSubsystemResponse.fromJSON(object.listCustomSubsystems)
        : undefined,
      call: isSet(object.call) ? CallResponse.fromJSON(object.call) : undefined,
    };
  },

  toJSON(message: Response): unknown {
    const obj: any = {};
    if (message.listCustomSubsystems !== undefined) {
      obj.listCustomSubsystems = ListCustomSubsystemResponse.toJSON(message.listCustomSubsystems);
    }
    if (message.call !== undefined) {
      obj.call = CallResponse.toJSON(message.call);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Response>, I>>(base?: I): Response {
    return Response.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Response>, I>>(object: I): Response {
    const message = createBaseResponse();
    message.listCustomSubsystems = (object.listCustomSubsystems !== undefined && object.listCustomSubsystems !== null)
      ? ListCustomSubsystemResponse.fromPartial(object.listCustomSubsystems)
      : undefined;
    message.call = (object.call !== undefined && object.call !== null)
      ? CallResponse.fromPartial(object.call)
      : undefined;
    return message;
  },
};

function createBaseNotification(): Notification {
  return { customNotification: undefined };
}

export const Notification = {
  encode(message: Notification, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.customNotification !== undefined) {
      CustomNotification.encode(message.customNotification, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Notification {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.customNotification = CustomNotification.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Notification {
    return {
      customNotification: isSet(object.customNotification)
        ? CustomNotification.fromJSON(object.customNotification)
        : undefined,
    };
  },

  toJSON(message: Notification): unknown {
    const obj: any = {};
    if (message.customNotification !== undefined) {
      obj.customNotification = CustomNotification.toJSON(message.customNotification);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Notification>, I>>(base?: I): Notification {
    return Notification.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Notification>, I>>(object: I): Notification {
    const message = createBaseNotification();
    message.customNotification = (object.customNotification !== undefined && object.customNotification !== null)
      ? CustomNotification.fromPartial(object.customNotification)
      : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  const bin = globalThis.atob(b64);
  const arr = new Uint8Array(bin.length);
  for (let i = 0; i < bin.length; ++i) {
    arr[i] = bin.charCodeAt(i);
  }
  return arr;
}

function base64FromBytes(arr: Uint8Array): string {
  const bin: string[] = [];
  arr.forEach((byte) => {
    bin.push(globalThis.String.fromCharCode(byte));
  });
  return globalThis.btoa(bin.join(""));
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
