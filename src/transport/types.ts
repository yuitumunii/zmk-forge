// A device offered by a transport's device picker (label shown in the UI,
// id passed back to connect). Previously lived in the removed Tauri shell.
export type AvailableDevice = { label: string; id: string };
